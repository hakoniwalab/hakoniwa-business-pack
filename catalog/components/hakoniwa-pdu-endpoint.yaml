id: hakoniwa-pdu-endpoint
name: Hakoniwa PDU Endpoint
repository:
  owner: hakoniwalab
  name: hakoniwa-pdu-endpoint
  url: https://github.com/hakoniwalab/hakoniwa-pdu-endpoint
  visibility: public
distribution:
  channel: oss
  notes:
    - Public OSS Hakoniwa component.
verification:
  verified_at: 2026-08-29
  source_revision: 9015a17415fd4a2042de2528b835a265af85b165
  source_ref: main
summary: Config-driven PDU endpoint layer that makes cache lifetime, communication transport, and PDU meaning explicit for distributed Hakoniwa simulations.
category:
  primary: communication
  secondary:
    - integration
    - infrastructure
    - language-binding

status:
  maturity: active
  catalog_confidence: high
  notes:
    - Cataloged from README, design philosophy, tutorial, config samples, examples, benchmark docs, build architecture, TCP runtime contract, and Core variant package CI.
    - This component is intentionally stricter and heavier than a minimal messaging library.
    - Core-aware consumers can choose explicit callback or polling package targets without changing the manifest schema.
    - Installed-package composability is verified with external CMake consumers on x64 and ARM64 platforms.
    - TCP and TCP mux lifecycle behavior is documented and covered by runtime-contract tests, including timeout, disconnect, reconnect, stop, and diagnostic behavior.

capabilities:
  - name: Explicit endpoint composition
    description: Separates cache, communication, and optional PDU definition so simulation semantics are configured instead of implied.
    evidence:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: README.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: docs/design_philosophy.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
  - name: Transport-swappable PDU communication
    description: Allows application code to switch between TCP, UDP, WebSocket, SHM, storage, Zenoh, RMW Zenoh, and MQTT through JSON configuration.
    evidence:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: README.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: docs/tutorials/endpoint.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
  - name: Cache semantics
    description: Supports latest-value state cache and queue-style event cache, making overwrite and receive-order behavior explicit.
    evidence:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: README.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: docs/receive_semantics.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
  - name: Replay and inspection through storage backend
    description: Writes PDU traffic to latest-slot or append-only queue files and provides a debug tool with human-readable and JSON output.
    evidence:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: README.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: docs/storage_comm.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
  - name: Native and language binding access
    description: Provides C++ endpoint APIs, C FFI-oriented implementation, Python bindings, and C# examples/tests for engine or tool integration.
    evidence:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: README.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: docs/python_binding.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: csharp/README.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
  - name: Explicit Hakoniwa Core frontend variants
    description: When Hakoniwa Core is enabled, new consumers can link callback/assets or polling/shakoc variants explicitly while the legacy all-in target remains available for compatibility.
    evidence:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: README.md
        revision: 5c2b815ae661f6063a91840c356717558dab9ab3
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: src/core_variants/CMakeLists.txt
        revision: 5c2b815ae661f6063a91840c356717558dab9ab3
  - name: Installed CMake package consumer contract
    description: Exports base, core_callback, and core_polling CMake targets and validates downstream find_package/link behavior with a separate external consumer project.
    evidence:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: README.md
        revision: 5c2b815ae661f6063a91840c356717558dab9ab3
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: test/package_consumer
        revision: 5c2b815ae661f6063a91840c356717558dab9ab3
  - name: Explicit TCP lifecycle and diagnostic contract
    description: Defines timeout, disconnect, reconnect, stop, and failed-session ownership for TCP and TCP mux, with connection-scoped diagnostics that preserve native socket errors and mapped Endpoint errors.
    evidence:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: docs/tcp-runtime-contract.md
        revision: 9015a17415fd4a2042de2528b835a265af85b165
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: test/tcp_runtime_contract_test.cpp
        revision: 9015a17415fd4a2042de2528b835a265af85b165

limitations:
  - Not a general-purpose messaging library.
  - Not optimized for the shortest possible Hello World path.
  - Requires explicit JSON configuration; implicit defaults are intentionally avoided.
  - SHM communication and Hakoniwa time source features require Hakoniwa Core, and new Core-aware consumers should choose either callback or polling package variants explicitly.
  - Python package depends on the native shared library and CFFI extension; it is not pip-only in the endpoint repository.
  - Transport-level reliability, persistence, and delivery semantics depend on the selected comm backend.
  - A positive TCP read or write timeout is a terminal error boundary for the current connection; the failed socket is closed rather than reused.
  - TCP diagnostic monotonic timestamps order events within one process and are not wall-clock timestamps for direct cross-host comparison.

interfaces:
  inputs:
    - name: Endpoint configuration
      kind: config
      description: JSON file linking cache, comm, and optional pdu_def settings.
    - name: Cache configuration
      kind: config
      description: JSON file defining latest or queue cache behavior.
    - name: Communication configuration
      kind: config
      description: JSON file defining protocol, role, address, port, storage path, pub/sub prefix, or transport timeout options.
    - name: PDU definition
      kind: config
      description: Optional JSON mapping PDU names to channel IDs, sizes, and type names.
    - name: PDU payload bytes
      kind: pdu
      description: Raw bytes sent or received by key.
  outputs:
    - name: PDU payload bytes
      kind: pdu
      description: Raw bytes delivered to local cache, transport, storage, or receive callbacks.
    - name: Storage files
      kind: file
      description: Latest-slot or append-only queue binary files for persistence, replay, and inspection.
    - name: Debug metadata
      kind: command
      description: Human-readable or JSON summaries from hako_pdu_storage_debug.
    - name: TCP transport diagnostics
      kind: log
      description: Connection-scoped failure diagnostics containing transport, role, connection ID, peer, operation, native socket error, mapped Endpoint error, and configured timeout when available.
  protocols:
    - name: TCP
      role: client | server
      notes: Reliable stream communication with explicit endpoint roles. Server endpoints return to accept after a failed connection, while clients return to the connect loop; positive blocking I/O timeout expiration terminates the current connection.
    - name: UDP
      role: unicast | broadcast | multicast
      notes: Connectionless communication for local or network PDU delivery.
    - name: Shared Memory
      role: local
      notes: High-performance local IPC with Hakoniwa Core integration; callback and polling are explicit consumer variants for new integrations.
    - name: WebSocket
      role: client | server
      notes: Stream-based communication suitable for browser-facing or network tool integration.
    - name: Storage
      role: file
      notes: Persistent latest or queue file backend for audit, replay, and offline inspection.
    - name: Zenoh
      role: pubsub
      notes: Dynamic peer-to-peer pub/sub through Zenoh key expressions.
    - name: RMW Zenoh
      role: pubsub
      notes: ROS middleware-oriented Zenoh integration.
    - name: MQTT
      role: pubsub
      notes: Broker-based pub/sub that maps PDU keys to MQTT topics.
  configuration:
    - path: config/schema/endpoint_schema.json
      description: Endpoint schema.
    - path: config/schema/cache_schema.json
      description: Cache schema.
    - path: config/schema/comm_schema.json
      description: Communication schema including TCP read and write timeout options.
    - path: config/schema/pdu_def_schema.json
      description: PDU definition schema.
    - path: config/tutorial/endpoint_tcp_server.json
      description: Tutorial TCP server endpoint.
    - path: config/tutorial/endpoint_tcp_client.json
      description: Tutorial TCP client endpoint.
    - path: config/tutorial/endpoint_udp_server.json
      description: Tutorial UDP server endpoint.
    - path: config/tutorial/endpoint_websocket_server.json
      description: Tutorial WebSocket server endpoint.

dependencies:
  required:
    - name: C++20 compiler
      type: tool
      notes: Required to build the native library and examples.
    - name: CMake 3.16 or later
      type: tool
      notes: Required build system.
    - name: Boost headers
      type: library
      notes: Required for networking implementation; tools/hako.py doctor probes Boost.Asio and Boost.Beast with a C++20 compile check on supported platforms.
  optional:
    - name: Hakoniwa Core Library
      type: repository
      notes: Required for SHM communication and Hakoniwa time source integration; exported package variants resolve the relevant Core frontend transitively.
    - name: Python 3 and cffi
      type: package
      notes: Required for Python FFI bindings.
    - name: GoogleTest
      type: library
      notes: Required for C++ test suite.
    - name: zenoh-c
      type: library
      notes: Required when Zenoh support is enabled.
    - name: Eclipse Paho MQTT C++
      type: library
      notes: Required when MQTT support is enabled.
    - name: .NET SDK
      type: runtime
      notes: Required for C# smoke tests and examples.

platforms:
  supported:
    - macOS
    - Linux
    - Windows
  notes:
    - Normal CI covers manifest-driven builds and transport combinations.
    - Core-variant package CI verifies external consumers on Ubuntu x64, native Linux ARM64, macOS, and Windows x64.
    - Hakoniwa Core integration CI validates Core-aware runtime composition.
    - Boost prerequisite diagnosis uses the same CMake/C++20 header compile probe across platforms; Windows may use a resolved vcpkg toolchain while native CMake discovery remains valid.
    - On Windows, Boost through vcpkg is recommended and Hakoniwa Core keeps its static-first library design.
    - The manifest's hakoniwa_core flag controls whether Core-aware artifacts are built; callback versus polling is selected by the downstream CMake target, not by the manifest.

connects_to:
  - component_id: hakoniwa-core-pro
    direction: uses
    interface: SHM / time source / core libraries
    notes: Needed for shared memory communication and Hakoniwa time integration; new consumers select assets through core_callback or shakoc through core_polling.
  - component_id: hakoniwa-pdu-registry
    direction: uses
    interface: PDU definition / type layout / generated codecs
    notes: Provides the PDU names, channel IDs, sizes, and type definitions that make bytes meaningful.
  - component_id: hakoniwa-pdu-bridge-core
    direction: used_by
    interface: Endpoint API
    notes: Core-free Bridge uses the base Endpoint package target, while Hakoniwa-integrated Bridge uses core_callback.
  - component_id: hakoniwa-pdu-rpc
    direction: used_by
    interface: Endpoint transport / PDU request-response channels
    notes: RPC layer reuses endpoint topology and transport configuration.
  - component_id: hakoniwa-conductor-light
    direction: used_by
    interface: TCP endpoint / RPC transport
    notes: Conductor-light examples use endpoint and PDU-RPC submodules for dynamic asset coordination.
  - component_id: hakoniwa-godot
    direction: used_by
    interface: endpoint config / PDU transport
    notes: Godot integration consumes endpoint configs for simulation synchronization and PDU exchange.
  - component_id: hakoniwa-pdu-foxglove
    direction: used_by
    interface: PduComm adapter
    notes: Foxglove adapter derives from endpoint communication abstraction.
  - component_id: hakoniwa-pdu-python
    direction: related
    interface: Python PDU integration
    notes: Separate Python-oriented PDU library; endpoint repository also provides its own Python FFI bindings.

data_contracts:
  - name: PduResolvedKey
    description: Addressing key based on robot name and channel ID, used across endpoint send and receive paths.
    source:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: docs/tutorials/endpoint.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
  - name: Cache semantics
    description: latest stores the current state per key; queue stores receive-order events up to configured depth.
    source:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: README.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
  - name: StorageHeader file format
    description: Self-describing binary storage files for latest and queue modes, inspectable by debug tooling.
    source:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: docs/storage_comm.md
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
  - name: PDU definition mapping
    description: Optional mapping from semantic PDU names to channel IDs, sizes, and type names.
    source:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: config/schema/pdu_def_schema.json
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
  - name: Installed Endpoint CMake targets
    description: Core-free consumers use hakoniwa_pdu_endpoint::hakoniwa_pdu_endpoint; new Core-aware consumers prefer hakoniwa_pdu_endpoint::core_callback or hakoniwa_pdu_endpoint::core_polling.
    source:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: README.md
        revision: 5c2b815ae661f6063a91840c356717558dab9ab3
  - name: TCP runtime contract
    description: Positive blocking I/O timeouts terminate the current TCP connection; normal TCP server/client endpoints recreate transport ownership through accept/connect loops, while failed TCP mux sessions are replaced by the upper layer. Stop-triggered shutdown is treated as expected lifecycle rather than abnormal disconnect.
    source:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: docs/tcp-runtime-contract.md
        revision: 9015a17415fd4a2042de2528b835a265af85b165

recipe_roles:
  - role: bridge
    description: Provides the transport endpoint used by bridges to move PDU data between assets or tools.
  - role: communication_infrastructure
    description: Standardizes how assets send, receive, cache, persist, and inspect PDU bytes.
  - role: integration_boundary
    description: Defines the causal boundary between simulation participants with explicit cache and delivery semantics.
  - role: language_binding
    description: Exposes endpoint behavior to Python and C# integration contexts through bindings and examples.

typical_usecases:
  - Multi-asset simulations where communication semantics must be explicit and auditable.
  - Swapping TCP, UDP, WebSocket, or pub/sub transports without changing application code.
  - Long-lived TCP simulation links where disconnect, timeout, reconnect, and diagnostic behavior must be explicit.
  - Local high-performance Hakoniwa asset communication through shared memory.
  - Persisting PDU traffic for replay, audit, debugging, or offline analysis.
  - Building higher-level bridge, RPC, visualization, or engine-integration components on a common PDU endpoint abstraction.
  - Integrating Endpoint as an installed CMake package without reconstructing include paths or Core library names manually.

anti_usecases:
  - A quick prototype that only needs a few implicit pub/sub messages.
  - A general web API or service mesh unrelated to Hakoniwa PDU semantics.
  - Workloads where JSON messages are sufficient and binary layout compatibility does not matter.
  - Systems where hidden defaults are preferred over explicit configuration.

demo_candidates:
  - name: Config-only transport swap
    description: Run endpoint_minimal over TCP, then UDP, then WebSocket using the same binary and different JSON configs.
    required_components:
      - hakoniwa-pdu-endpoint
    source_examples:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: examples/endpoint_minimal.cpp
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: config/tutorial
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
  - name: Storage replay and inspection
    description: Send packets through storage latest and queue endpoints, then inspect binary files with hako_pdu_storage_debug and JSON output.
    required_components:
      - hakoniwa-pdu-endpoint
    source_examples:
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: examples/endpoint_storage_latest.cpp
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
      - repository: hakoniwalab/hakoniwa-pdu-endpoint
        path: examples/endpoint_storage_queue.cpp
        revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
  - name: Hakoniwa bridge transport backend
    description: Use endpoint as the I/O layer under a PDU bridge recipe where bridge-core controls transfer timing; use core_callback for the Hakoniwa callback Web bridge path.
    required_components:
      - hakoniwa-pdu-endpoint
      - hakoniwa-pdu-bridge-core
    source_examples:
      - repository: hakoniwalab/hakoniwa-pdu-bridge-core
        path: README.md
        revision: e2454b15c22466336df565af09db9f43d2066da7

operations:
  build:
    commands:
      - python tools/hako.py doctor
      - python tools/hako.py configure --dry-run
      - python tools/hako.py build
      - python tools/hako.py test
    notes:
      - hakoniwa-build.yaml is the recommended user-facing source of build intent across Windows, macOS, and Linux.
      - Direct CMake remains available for advanced or compatibility workflows.
      - features.hakoniwa_core controls whether Core-aware artifacts exist; consumers choose callback or polling by linking the corresponding exported target.
  test:
    commands:
      - python tools/hako.py test
      - ctest --test-dir build --output-on-failure
      - bash test-python.bash
      - bash test-csharp.bash
    notes:
      - C++ tests use GoogleTest.
      - Python and C# smoke tests are provided by helper scripts.
      - core-variants CI builds a separate external CMake consumer to validate the installed package contract.
      - TCP runtime-contract tests cover timeout mapping, disconnect/reconnect ownership, stop behavior, and socket portability semantics.
  run:
    commands:
      - build/examples/endpoint_minimal config/tutorial/endpoint_tcp_server.json recv 10
      - build/examples/endpoint_minimal config/tutorial/endpoint_tcp_client.json send 5
      - build/tools/hako_pdu_storage_debug path/to/storage_queue.bin --json
    notes:
      - Tutorial examples require building with HAKO_PDU_ENDPOINT_BUILD_EXAMPLES=ON.

known_gaps:
  - Catalog does not yet include exact schema field-level details for every comm backend.
  - Catalog does not yet distinguish stable release tags from main-branch-only features.
  - Full downstream runtime compatibility still needs recipe-level validation even though the installed package/link contract is CI-verified.
  - Performance characteristics should be selected from benchmark reports when a recipe has latency or throughput requirements.

source_refs:
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: README.md
    revision: 9015a17415fd4a2042de2528b835a265af85b165
    description: Main overview, manifest flow, Core variants, installed package targets, supported platforms, prerequisite diagnosis, and consumer guidance.
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: docs/build-architecture.md
    revision: 9015a17415fd4a2042de2528b835a265af85b165
    description: Manifest resolution and cross-platform build architecture.
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: docs/tcp-runtime-contract.md
    revision: 9015a17415fd4a2042de2528b835a265af85b165
    description: TCP and TCP mux timeout, disconnect, reconnect, stop, failed-session ownership, and diagnostic contract.
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: .github/workflows/core-variants.yml
    revision: 9015a17415fd4a2042de2528b835a265af85b165
    description: Core package, explicit frontend variants, and external consumer validation across x64 and ARM64 platforms.
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: .github/workflows/hako-core-integration.yml
    revision: 9015a17415fd4a2042de2528b835a265af85b165
    description: Hakoniwa Core integration validation for Core-aware Endpoint composition.
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: docs/design_philosophy.md
    revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
    description: Design intent and scope boundary.
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: docs/tutorials/endpoint.md
    revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
    description: TCP, UDP, and WebSocket tutorial.
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: docs/storage_comm.md
    revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
    description: Storage communication behavior.
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: docs/receive_semantics.md
    revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
    description: Receive behavior and cache semantics.
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: benchmarks/PERFORMANCE.md
    revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
    description: TCP and SHM callback performance guidance.
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: config/schema
    revision: 9015a17415fd4a2042de2528b835a265af85b165
    description: JSON schemas for endpoint, cache, comm, and PDU definitions, including TCP timeout configuration.
  - repository: hakoniwalab/hakoniwa-pdu-endpoint
    path: examples
    revision: 5e8d412dca9dcf6c41456d273968714b077e2e99
    description: C++ examples for transport, storage, and pub/sub variants.

tags:
  - hakoniwa
  - pdu
  - endpoint
  - tcp
  - udp
  - websocket
  - shm
  - storage
  - zenoh
  - mqtt
  - cplusplus
  - python
  - csharp
